<Window x:Class="Askart.Windows.PinnedMessagesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Закреплённые сообщения" 
        Height="500" 
        Width="600"
        MinWidth="400"
        MinHeight="300"
        Icon="/Resources/logo.png"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}"
        ResizeMode="CanResize">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" Background="{StaticResource ActiveBrush}" Padding="20">
            <Grid>
                <TextBlock Text="📌 Закреплённые сообщения" 
                           FontFamily="{StaticResource Roboto}"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="White"
                           VerticalAlignment="Center"/>
                <TextBlock x:Name="ChatNameText" 
                           FontFamily="{StaticResource Roboto}"
                           FontSize="12"
                           Foreground="White"
                           Opacity="0.9"
                           VerticalAlignment="Bottom"/>
            </Grid>
        </Border>

        <!-- Список закреплённых -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="20">
            <StackPanel x:Name="PinnedPanel"/>
        </ScrollViewer>

        <!-- Кнопка закрытия -->
        <Border Grid.Row="2" Background="{StaticResource CardBackgroundBrush}" Padding="15">
            <Button Content="Закрыть"
                    Style="{StaticResource StandardButtonStyle}"
                    HorizontalAlignment="Center"
                    Padding="30,10"
                    Click="Close_Click"/>
        </Border>
    </Grid>
</Window>