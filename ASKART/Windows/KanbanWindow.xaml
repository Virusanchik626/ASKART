<Window x:Class="Askart.Windows.KanbanWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Канбан-доска - Askart" 
        Height="700" 
        Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}"
        Icon="/Resources/logo.png">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" Background="{StaticResource ActiveBrush}" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="Канбан-доска" 
                               FontFamily="{StaticResource Roboto}"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="White"/>
                    <TextBlock x:Name="ChatNameText" 
                               FontFamily="{StaticResource Roboto}"
                               FontSize="14"
                               Foreground="White"
                               Opacity="0.9"/>
                </StackPanel>

                <Button Grid.Column="1"
                        Content="+ Новая задача"
                        Background="White"
                        Foreground="{StaticResource ActiveBrush}"
                        Padding="15,8"
                        BorderThickness="0"
                        
                        FontFamily="{StaticResource Roboto}"
                        FontWeight="SemiBold"
                        Cursor="Hand"
                        Click="AddTask_Click"/>
            </Grid>
        </Border>

        <!-- Колонки -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Новая -->
            <Border Grid.Column="0" Background="{StaticResource SecondaryBackgroundBrush}" CornerRadius="12" Margin="0,0,10,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Padding="15" Background="{StaticResource InactiveTextBrush}" CornerRadius="12,12,0,0">
                        <TextBlock Text="Новая" 
                                   FontFamily="{StaticResource Roboto}"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   Foreground="White"/>
                    </Border>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="NewTasksPanel" Margin="10"/>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- В работе -->
            <Border Grid.Column="1" Background="{StaticResource SecondaryBackgroundBrush}" CornerRadius="12" Margin="5,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Padding="15" Background="{StaticResource ActiveBrush}" CornerRadius="12,12,0,0">
                        <TextBlock Text="В работе" 
                                   FontFamily="{StaticResource Roboto}"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   Foreground="White"/>
                    </Border>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="InProgressTasksPanel" Margin="10"/>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- Выполнена -->
            <Border Grid.Column="2" Background="{StaticResource SecondaryBackgroundBrush}" CornerRadius="12" Margin="10,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Padding="15" Background="{StaticResource SuccessBrush}" CornerRadius="12,12,0,0">
                        <TextBlock Text="Выполнена" 
                                   FontFamily="{StaticResource Roboto}"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   Foreground="White"/>
                    </Border>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="CompletedTasksPanel" Margin="10"/>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>